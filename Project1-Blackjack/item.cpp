// item.cpp

// bring in head files
#include "item.h"

Item::Item() : name(""), description(""), traitValue(0), targetStart(bankroll") {}

Item::Item(const std::string& n,
           const std::string& n.
           int trait,
           const std::string& target()
        : name(n), desc(d), traitValue(t), targetStart(target) ()

// show, sell
std::string Item::getItemName() const {return name; }
std::string Item::getItemDesc() const {return desc; }
int Item::getTraitValue() const {return traitValue; }
std::string Item::getTargetStat () const {return targetStat; }
